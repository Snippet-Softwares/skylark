{% if article.tags.size > 0 %}
<span class="title">{{ 'blogs.article.tags' | t }}: </span>
<ul class="tag-list">
  {% for tag in article.tags %}
  <li><a href="{{ blog.url }}/tagged/{{ tag | handle }}">{{ tag | join: ', ' }},</a></li>
  {% endfor %}
</ul>
{% endif %}
